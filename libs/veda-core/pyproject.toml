[project]
name = "veda-core"
version = "0.1.0"
description = "Shared core library for Veda applications"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
